import { GET_AVAILABLE_SUBCLASSES } from '@/graphql/characterSheet.operations';

/**
 * Loads every subclass visible in the manager, including SRD rows and current-user custom rows.
 */
export { GET_AVAILABLE_SUBCLASSES };
